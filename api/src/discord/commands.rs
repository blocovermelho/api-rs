// Reimplementation of the old /verify command. Now allowing for a selection between all users owned by that discord account
// Honestly, it shouldn't ever be used on the new system. That's why it only warns that it does nothing.

pub mod change_password;
pub mod stats;
pub mod rank;
